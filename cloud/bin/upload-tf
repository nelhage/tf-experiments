#!/bin/bash
image="$1"
version="${image#*:}"

gcloud auth activate-service-account "--key-file=$GOOGLE_APPLICATION_CREDENTIALS"

gsutil -o 'GSUtil:parallel_composite_upload_threshold=100M' \
       cp -r -m /data/ "gs://nelhage-ml/pong/$(date +%s)-$version/"
